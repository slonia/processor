encoding "utf8";

TTextMinerConfig {
  Dictionary = "dates.gzt";

  PrettyOutput = "debug.html";

  Input = {
    File = "input.txt";
  }

  Articles = [
    { Name = "РезультатДата" }
  ]

  Facts = [
    { Name = "Date" }
  ]

  Output = {
    File = "output.txt";
    Format = text;
  }
}
